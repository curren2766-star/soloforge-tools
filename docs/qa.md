# 検証記録

検証日：2026-09-07。Node.js 24.19.0、Windows上のMicrosoft Edge（Playwright / headless）。

- ビルド：8ページとsitemapを正常生成。実行時の外部パッケージなし。
- 単体・静的検証：22件成功。指定の参考5条件、全色形式×色深度、HDR、DSC、85%・実効上限の境界、TMDS 4:2:2、不正・最大入力、JSON-LD、canonical、内部リンク、robots、所有権確認ファイル。
- 実ブラウザ：170項目成功。1440 / 768 / 390 / 320pxで全ページの横はみ出しなし、200%文字拡大、入力ラベル、キーボードのスキップリンク、深いURLの404、JavaScript例外なし。
- 新ツール：初期4K144 RGB10 = 35.83Gbps表示、コピーは35.831808Gbps。UWQHDプリセット、カスタム解像度・小数Hz、DSC条件付き、異常入力で古い結果・比較・コピーを無効化。
- OBS：初期54.43GB、16本、上り4時間45分。8Mbps・音声0・1時間で3.60GB。空欄・0分・60分入力を拒否し、30分へ戻すと1.80GBに復帰。コピー成功。
- GA4：ローカルから外部通信ゼロ。本番origin代替＋通信interceptにより、初期toolイベントなし、start1回、同条件complete重複なし、コピー成功時share、内部・仮リンクで広告イベントなし、外部広告クリックのみ計測を検証。入力値をイベントに含めない。?analytics=offでローダー停止。
- 外部リンク：本文の12URLすべてHTTP 200を確認。GitHub Issuesは有効。
- 見た目：トップ、OBS、新ツールのPC・スマホ画像を確認。画像はローカルの.qa/に保存し公開コミットから除外。

## 検証の限界

実GPU・モニター・ケーブルでの動作検証はしていない。タイミングや圧縮条件を確定するツールではない。Safari / Firefox / 実スマホ端末・スクリーンリーダー読み上げによる検証は未実施。GA4管理画面で新イベントの実受信確認はしていない（テストで本番解析を汚さないため）。Search Consoleの設定は変更していない。
