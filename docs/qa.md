# 検証記録

検証日：2026-09-07。Node.js 24.19.0、Windows上のGoogle Chrome（Playwright / headless）。

- ビルド：11ページとsitemapを正常生成。実行時の外部パッケージなし。
- 単体・静的検証：52件成功。字幕の代表CPS・未丸め境界・書記素・改行・表示時間・入力境界、PPI、既存の帯域式・GPU電源判定、JSON-LD、canonical、内部リンク、robots、所有権確認ファイル。
- 実ブラウザ：263項目成功。1440 / 768 / 390 / 320pxで全ページの横はみ出しなし、200%文字拡大、入力ラベル、キーボードのスキップリンク、深いURLの404、JavaScript例外なし。
- 字幕読み速度：20文字 / 5秒 = 4.0 CPS、書記素クラスタと改行除外、表示時間の0.1秒切り上げ、0秒で結果消去、正常値への復帰を確認。
- SEO・クロール：公開10 URLのtitle・description固有性、canonical・OGP一致、H1と見出し順、全5ツールのFAQ表示／schema一致、sitemap完全一致、トップからの到達性、関連クラスタ、404 noindex、軽量アセットを確認。
- モニターPPI：初期27インチWQHD = 108.8 PPI、32インチ4K = 137.7 PPI、差26.6%。34インチUWQHDプリセット、同一／ほぼ同じ／低い表示、空欄・0・小数ピクセル・上限超過、4K・高Hzチェッカーへの導線を確認。
- 接続チェッカー：初期4K144 RGB10 = 35.83Gbps表示、コピーは35.831808Gbps。UWQHDプリセット、カスタム解像度・小数Hz、DSC条件付き、異常入力で古い結果・比較・コピーを無効化。
- OBS：初期54.43GB、16本、上り4時間45分。8Mbps・音声0・1時間で3.60GB。空欄・0分・60分入力を拒否し、30分へ戻すと1.80GBに復帰。コピー成功。
- GA4：ローカルから外部通信ゼロ。本番origin代替＋通信interceptにより、5ツールとも初期toolイベントなし、start1回、同条件complete重複なし、字幕本文・秒数を含む入力値をイベントに含めないことを確認。既存ツールのコピー成功時share、内部・仮リンクで広告イベントなし、外部広告クリックのみ計測、?analytics=offでローダー停止も維持。
- 外部リンク：本文の12URLすべてHTTP 200を確認。GitHub Issuesは有効。
- 見た目：トップ、字幕、PPI、GPU、接続、OBSのPC・スマホ画像を確認。画像はローカルの.qa/に保存し公開コミットから除外。

## 検証の限界

実GPU・モニター・ケーブルでの動作検証はしていない。タイミングや圧縮条件を確定するツールではない。Safari / Firefox / 実スマホ端末・スクリーンリーダー読み上げによる検証は未実施。GA4管理画面で新イベントの実受信確認はしていない（テストで本番解析を汚さないため）。Search Consoleの設定は変更していない。
